import React from 'react';
import { TabsData } from '../data/userData';

const Tabs = () => {
  console.log(TabsData);
  return (
    <div>
      <h1>Tabs</h1>
    </div>
  );
};

export default Tabs;
